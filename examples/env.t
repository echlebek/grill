Check environment variables:

  $ echo "$LANG"
  C
  $ echo "$LC_ALL"
  C
  $ echo "$LANGUAGE"
  C
  $ echo "$TZ"
  GMT
  $ echo "$CDPATH"
  
  $ echo "$GREP_OPTIONS"
  
  $ echo "$CRAMTMP"
  .+ (re)
  $ echo "$TESTDIR"
  **/examples (glob)
  $ ls "$TESTDIR"
  env.t

# TODO re-enable when files are added back
# bare.t
# empty.t
# fail.t
# missingeol.t
# skip.t
# test.t

  $ echo "$TESTFILE"
  env.t

  $ pwd
  **/grilltests*/env.t (glob)
